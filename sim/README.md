# L4 — simulation

Runners for the oracle ladder.

- **L1 spike** — `spike --extension=gemmini` against `software/libgemmini`. Bootstrap only
  (`derived_from_rtl: false`); wraps the build recipe in `planning/...bridge_plan.md` section 6.1.
- **L2 Verilator** — `MxGemminiRocketConfig`. The first RTL-certified tier. Later.

Populated in Step 6.
