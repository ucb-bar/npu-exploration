out/
.venv/
radiance-kernels/

# provisioned by scripts/setup.sh (not vendored; MXQuant may be a symlink)
toolchain/
MXQuant
__pycache__/
*.pyc

# run outputs (regenerated on demand)
results/

# local notes and parked work — not for upstream
ENGAGING_NOTES.md
PLAN.md
parked/

# Generated kernel data: regenerate with `make -C baremetal/mxgemmini data`.
# ~22 MB of derived bytes, and tracking them is how llama_attn.h sat stale through a
# rounding-mode change while every test still passed (planning/llama_layer_hw_plan.md 9.1).
baremetal/*/data/
